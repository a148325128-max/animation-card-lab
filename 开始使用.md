# 动画卡片实验室 · 本地试用 0.2

这是一个本地源码试用包，可选卡、修改内容、手工拉片并导出MP4。当前内置4种卡片：大字强调、数据对比、三步流程、目录转章节。没有接入自动识别视频结构或自动仿写；不会调用收费模型。试用包没有账号、支付或云端上传功能。

本版本源码公开、免费使用，不采用标准开源许可。允许正常编辑卡片和制作、发布、商业使用成片；禁止修改程序源码或二次出售工具、源码和内置模板。完整范围及导出HTML的播放代码例外见 [LICENSE.md](LICENSE.md)；第三方软件说明见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。GitHub 平台允许的查看和 Fork 不代表额外取得修改或转售授权。

## 第一次打开

本轮验证环境为macOS。需要已安装 **Python 3.10或更新版本** 和 **FFmpeg（含ffprobe）**。不需要Node、Playwright、Codex或Claude。

- Python官方下载：https://www.python.org/downloads/macos/
- FFmpeg官方下载指引：https://ffmpeg.org/download.html
- 已安装Homebrew的用户可运行 `brew install python ffmpeg`。

本包尚未内置这些运行时，不能视为任何新电脑都免安装可用。依赖安装好之后：

1. 解压到有写入权限的本地文件夹，不要在ZIP内直接运行。
2. 双击 `打开动画卡片实验室.command`。
3. 如果macOS要求确认文件来源，可在Finder中右键打开。浏览器会打开本地工作台；环境缺失时会明确说明。
4. 可重新双击启动器打开已有服务；端口被另一份程序占用时会选用其他空闲端口，不会打开别的副本或停止其他服务。

## 直接制作一张动画

1. 在「使用卡片」中选一张。
2. 改标题、数字或步骤，也可上传自己的Logo。
3. 播放查看；画幅下拉可切换横屏或竖屏。精细参数在「高级调整」。
4. 点击「导出MP4视频」，保持页面打开。完成后点击下载链接。
5. 点击右上角「保存到卡片库」，下次可继续编辑。保存卡片不强制要求参考片。

MP4为720p横/竖、24帧每秒、无声、实色背景；透明背景和声音合成尚未接入。导出会保存按钮点击时的内容快照，逐帧生成并校验。改内容后需要重新导出，不会自动更新旧文件。

## 从视频学习动作

1. 打开「拉片制作」，选择本地MP4、MOV或WebM（≤250MB）。优先使用自己的视频或有权使用的素材。
2. 输入起止秒数，单次最多12秒；点击「抽帧并保存选段」。
3. 查看15张带源时间的抽帧，在观察框记录元素与动作先后。
4. 手工选择重建结构，修改内容，并在高级调整里调整动作。抽帧不会自动恢复作者源码、图层、字体或精确缓动。
5. 使用「同播对照」看效果，确认后保存卡片、导出视频。

如果视频出现格式错误，可尝试导出成常规H.264 MP4再导入。清晰度不同、系统字体不同可能使排版发生变化，导出前请检查。

## 文件在哪里

- `data/library/`：你保存的卡片，各次保存保留独立版本。
- `data/imports/`：导入视频的本地副本，原文件不会修改。
- `data/references/`：选段、抽帧和观察证据。
- `data/exports/`：导出视频、参数快照、逐帧图像及校验信息。
- `qa/server-state.json`：实际打开的本机地址。

分享某一张卡可使用「更多导出格式」里的JSON/HTML；分享整个工作目录会包含个人导入素材，请自行选择分享内容。本次提供的ZIP仅含程序、说明和空数据目录，不含开发者的参考视频、Cookie、卡库、账户或测试记录。

本版本完成了同一台Mac的解压隔离目录验证；尚未完成另一台真实电脑和Windows/Linux验证。界面与可用性仍需外部试用者反馈。

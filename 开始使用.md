# 开始使用 · v0.4

## 第一步：启动

安装Python3.10+与FFmpeg（含ffprobe），双击打开动画卡片实验室.command。若系统未允许运行，可在本目录终端执行 `python3 scripts/open-lab.py`。启动器检查依赖，缺少时说明原因；默认仅本机访问。

## 第二步：选择免费模板

首页左边为12种基础卡片，下面“高级动画 · 免费”有毛玻璃/图标环绕、纵向轮播、光流片头3个入口。它们与手工拉片均免费。

基础卡片：改内容→预览→保存卡库或导出。高级动画：在各自编辑页换图/改字/调参数，用“保存参数”下载JSON，下次“读取参数”恢复；可从顶部回到卡片库。

环绕的无文字图标版可以上传六张自己的PNG/JPEG/WebP图标，每张≤1MB；中央主体≤4MB，透明背景效果更自然。本工具不会自动抠图。轮播三图每张≤4MB。默认自制抽象图标和风景图均可替换。

## 第三步：高级动画MP4导出环境

基础卡片导出只需浏览器与FFmpeg。高级三模板使用浏览器逐帧渲染，需要额外安装Node.js20+（含npm）；随后在解压目录执行：

```sh
python3 scripts/setup-advanced.py
```

脚本将固定版本Playwright1.62.1装在本目录.runtime，不改全局npm。优先使用本机Chrome；没有可用浏览器时下载Chromium，因此需网络。可用ANIMATION_CARD_BROWSER指定兼容Chromium浏览器可执行文件。回到页面刷新，环境栏显示就绪后导出。

高级视频无声、30fps。环绕有3:4/9:16/16:9；轮播和光流为720×1280。渲染逐帧进行，时长与电脑性能有关，请等下载链接出现。改动参数后旧下载链接隐藏，避免把旧视频当新结果。

## 免费拉片

首页切到“拉片制作”，选择MP4/MOV/WebM（≤250MB），填起止时间（每段≤12秒），抽15帧后观察与记录。选段、抽帧、参考元数据保存在data/。动画结构目前由人判断和手工选择，没有自动AI复刻。

## 文件与限制

数据在data/，安装组件在.runtime；请保留自己的参数JSON。公开发布请只用官方分发包，不上传个人使用目录。高级模板参数与基础卡片库分开保存。当前Mac同机验证，未验收其他电脑或Windows/Linux；这不是免安装应用。

查看LICENSE.md。允许正常换字换图和商业使用成片，不允许转售工具/源码/可复用模板包。智能体协助安装时先读AGENTS.md。
